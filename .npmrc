# Fail CI-style installs if the lockfile is out of date.
prefer-frozen-lockfile=true
# Never auto-install a mismatched pnpm; the packageManager field is authoritative.
manage-package-manager-versions=false
